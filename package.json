{
  "name": "sails-generate-new-with-mast",
  "version": "0.11.2",
  "description": "Override for `sails new` that sets up a new project w/ Mast.",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha"
  },
  "keywords": [
    "new",
    "generator",
    "sails",
    "generate",
    "plugin"
  ],
  "author": "rachaelraygun",
  "license": "MIT",
  "dependencies": {
    "lodash": ">=2.4.x",
    "merge-defaults": ">=0.1.0",
    "async": "~0.2.10",
    "fs-extra": "~0.8.1"
  },
  "devDependencies": {
    "sails-generate": "*",
    "reportback": "*",
    "fs-extra": "*"
  },
  "sailsGenerator": {
    "type": "new",
    "behavior": "overrides `sails generate new`",
    "sailsVersion": "~0.10.0"
  }
}
